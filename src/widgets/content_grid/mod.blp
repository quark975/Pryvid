using Gtk 4.0;
using Adw 1;

template $ContentGrid : Box {
  $ResultPage result_page {
    refreshable: true;
    child: FlowBox flowbox {
      vexpand: false;
      valign: start;
      selection-mode: none;
    };
  }
}
