using Gtk 4.0;
using Adw 1;

template $ContentGrid : Box {
  ScrolledWindow {
    vexpand: true;
    hexpand: true;

    FlowBox flowbox {
      vexpand: false;
      valign: start;
      selection-mode: none;
    }
  }
}
